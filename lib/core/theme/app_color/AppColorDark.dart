import 'package:flutter/material.dart';

/// SecureMail — Dark Color Palette
/// All values extracted from Figma design system
class AppColorDark {
  AppColorDark._();

  // ── Background ────────────────────────────────────────────
  static const Color background = Color(0xFF030D0A);

  // ── Buttons ───────────────────────────────────────────────
  static const Color button1 = Color(0xFF1F8A70);
  static const Color button2 = Color(0xFF145C4A);

  // ── Text ──────────────────────────────────────────────────
  static const Color text1 = Color(0xFFE8F6F2); // primary text
  static const Color text2 = Color(0xFFB7D9D1); // secondary text
  static const Color text3 = Color(0xFF7FA9A1); // muted text
  static const Color text4 = Color(0xFF2EC4A6); // link / active

  // ── Lamp / Overlay ────────────────────────────────────────
  static const Color lamp = Color(0xFF0F2A23);

  // ── Cards ─────────────────────────────────────────────────
  static const Color card1 = Color(0xFF0B1F1A);
  static const Color card2 = Color(0xFF0F2A23);
  static const Color card3 = Color(0xFF12352C);

  // ── Input Fields ──────────────────────────────────────────
  static const Color fieldBackground = Color(0xFF0F2A23);
  static const Color fieldBorder = Color(0xFF1F8A70);
  static const Color fieldPlaceholder = Color(0xFF6FA39A);
  static const Color fieldText = Color(0xFFE8F6F2);

  // ── Bottom Navigation Bar ─────────────────────────────────
  static const Color barBackground = Color(0xFF081712);
  static const Color barActiveIcon = Color(0xFF2EC4A6);
  static const Color barInactiveIcon = Color(0xFF6FA39A);
  static const Color securityReport1 = Color(0xFF6b1111);
  static const Color securityReport2 = Color(0xFF8f3b32);
  static const Color securityReport3 = Color(0xFFb4dbb8);

  // ── Semantic Colors ───────────────────────────────────────
  static const Color danger = Color(0xFFFF5252);
  static const Color success = Color(0xFF2EC4A6);
  static const Color warning = Color(0xFFFBC02D);
  static const Color info = Color(0xFF40C4FF);
}

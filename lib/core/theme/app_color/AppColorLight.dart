import 'package:flutter/material.dart';

/// SecureMail — Light Color Palette
/// All values extracted from Figma design system
class AppColorLight {
  AppColorLight._();

  // ── Background ────────────────────────────────────────────
  static const Color background = Color(0xFFF2FBF7);

  // ── Buttons ───────────────────────────────────────────────
  static const Color button1 = Color(0xFF1F8A70);
  static const Color button2 = Color(0xFFD6EEE6);

  // ── Text ──────────────────────────────────────────────────
  static const Color text1 = Color(0xFF0B1F1A); // primary text
  static const Color text2 = Color(0xFF145C4A); // heading accent
  static const Color text3 = Color(0xFF5F8079); // secondary text
  static const Color text4 = Color(0xFF1F8A70); // link / active

  // ── Cards ─────────────────────────────────────────────────
  static const Color card1 = Color(0xFFFFFFFF);
  static const Color card2 = Color(0xFFE6F5F0);
  static const Color card3 = Color(0xFFD6EEE6);

  // ── Input Fields ──────────────────────────────────────────
  static const Color fieldBackground = Color(0xFFFFFFFF);
  static const Color fieldBorder = Color(0xFFBFE3DA);
  static const Color fieldPlaceholder = Color(0xFF7FA9A1);
  static const Color fieldText = Color(0xFF0B1F1A);

  // ── Bottom Navigation Bar ─────────────────────────────────
  static const Color barBackground = Color(0xFFFFFFFF);
  static const Color barActiveIcon = Color(0xFF1F8A70);
  static const Color barInactiveIcon = Color(0xFF6FA39A);
  static const Color securityReport1 = Color(0xFF822626);
  static const Color securityReport2 = Color(0xFFb34236);
  static const Color securityReport3 = Color(0xFFd3f2d6);
}
